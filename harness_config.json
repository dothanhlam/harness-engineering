{
  "ba": {
    "_comment": "BA agent: runs business analysis & drafts definitions_of_done.md. Uses 'ollama run <model_name> <prompt>'",
    "agent": "ollama",
    "model_name": "hermes3:8b",
    "cmd_template": ["run", "{model}", "{prompt}"]
  },
  "_dev_claude_backup": {
    "_comment": "INACTIVE — Previous dev agent: Claude CLI. Swap with 'dev' key to restore.",
    "agent": "claude",
    "model_name": "claude-sonnet-4-20250514",
    "cmd_template": ["--print", "{prompt}", "--model", "{model}", "--dangerously-skip-permissions", "--add-dir", "./workspace", "--add-dir", "./memory"],
    "mcp_config": ""
  },
  "dev": {
    "_comment": "Dev agent: local ollama CLI.",
    "agent": "ollama",
    "model_name": "gemma4:e4b",
    "cmd_template": ["run", "{model}", "{prompt}"]
  },
  "devops": {
    "_comment": "DevOps agent: local ollama CLI. model_name is passed as 'ollama run <model_name>'.",
    "agent": "ollama",
    "model_name": "hermes3:8b",
    "cmd_template": ["run", "{model}", "{prompt}"]
  }
}
